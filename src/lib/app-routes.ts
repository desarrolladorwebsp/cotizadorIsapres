export const COTIZADOR_HOME = "/";

/** Rutas de página conocidas (sin barra final). */
export const KNOWN_PAGE_ROUTES = new Set([
  COTIZADOR_HOME,
  "/cotizador",
  "/cotizador/ejecutivos",
  "/cotizador/admin",
]);

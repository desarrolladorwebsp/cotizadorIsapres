# Neon PostgreSQL (en Vercel usa la URL con pooler)
DATABASE_URL="postgresql://USER:PASSWORD@HOST-pooler.REGION.aws.neon.tech/neondb?sslmode=require"
# Opcional: conexión directa solo para migraciones locales (`npm run db:migrate`)
# En Vercel, si migrate deploy falla en build, usa la URL directa (sin -pooler) en DATABASE_URL
# o configura DIRECT_URL en schema.prisma.
# DATABASE_URL_UNPOOLED="postgresql://USER:PASSWORD@HOST.REGION.aws.neon.tech/neondb?sslmode=require"

# Autenticación (mínimo 32 caracteres; generar con: openssl rand -base64 48)
AUTH_SECRET=

# URL pública del sitio (links en correos, widget y deep links)
# Desarrollo local: http://localhost:3001
NEXT_PUBLIC_APP_URL=https://cotizadorpremium.cl
APP_BASE_URL=https://cotizadorpremium.cl
# premium = /cotizador?agent= | legacy = /{slug}?entidad= (cotizador.cotizaloantes.cl)
NEXT_PUBLIC_COTIZADOR_ROUTING=premium

# Contraseña inicial para cuentas demo del seed (solo desarrollo)
SEED_ACCOUNT_PASSWORD=Cotizalo2026**

# Cuentas admin (referencia — login: /cotizador/admin/login)
# admin@isaprespremium.cl
# superadmin@isaprespremium.cl
# Usan la contraseña de SEED_ACCOUNT_PASSWORD o la clave temporal enviada por correo.

# API pública para integradores / agentes (mínimo 32 caracteres)
PUBLIC_API_SECRET=
# Opcional: URL base documentada en /api/public/v1/docs (ej. https://cotizador.cotizaloantes.cl/api/public/v1)
# PUBLIC_API_BASE_URL=

# Guía UI cards de planes (sin auth): GET /api/public/v1/ui/plan-card?brand=cotizalo-antes

# Entidad aliada por defecto en /cotizador (cotizadorpremium.cl)
DEFAULT_PARTNER_ENTITY_SLUG=cotizadorpremium

# Agent key del widget en la Landing (Cotizador Premium)
NEXT_PUBLIC_LANDING_AGENT_KEY=cotizadorpremium
# URL del script del widget embebible
# NEXT_PUBLIC_COTIZADOR_WIDGET_URL=/cotizador-widget.js

# Orígenes permitidos para iframe embebido (/cotizador?embed=1, /embed/*).
# Por defecto: socios conocidos + localhost en dev (NO usa *).
# Para abrir a cualquier sitio (no recomendado en prod): EMBED_FRAME_ANCESTORS=*
# EMBED_FRAME_ANCESTORS=https://cotizaloantes.cl https://www.cotizaloantes.cl

# Orígenes permitidos para POST públicos (/api/quotes, /api/cotizacion-notify).
# Opcional: agrega dominios extra separados por espacio o coma.
# ALLOWED_PUBLIC_ORIGINS=https://mi-sitio.cl

# --- Correos (Resend) — dominio cotizadorpremium.cl ---
RESEND_API_KEY=
# Usuario que cotiza (FROM en correos al cliente)
RESEND_COTIZACION_FROM_EMAIL=Cotizador Premium <cotizaciones@cotizadorpremium.cl>
# Staff e invitaciones (FROM en correos al equipo / ejecutivos)
RESEND_EQUIPO_FROM_EMAIL=Cotizador Premium <equipo@cotizadorpremium.cl>
# Buzón que recibe alertas de nuevas cotizaciones (TO)
EQUIPO_NOTIFY_EMAIL=equipo@cotizadorpremium.cl

# --- PDFs de planes ---
# Local (desarrollo sin Blob): disco en storage/planes-pdf
# PLAN_PDF_STORAGE=local

# Vercel Blob (producción y carga masiva local)
# Crear en: Vercel → Storage → Blob → Connect to project
# PLAN_PDF_STORAGE=blob
# BLOB_READ_WRITE_TOKEN=
# BLOB_STORE_ID=

# cPanel / FTP (hosting propio; evita cuota de Vercel Blob)
# PLAN_PDF_STORAGE=cpanel
# CPANEL_FTP_HOST=ftp.tudominio.cl
# CPANEL_FTP_USER=
# CPANEL_FTP_PASSWORD=
# CPANEL_FTP_REMOTE_DIR=public_html/planes-pdf
# CPANEL_PDF_PUBLIC_BASE_URL=https://tudominio.cl/planes-pdf
# CPANEL_FTP_SECURE=true
# CPANEL_FTP_VERBOSE=0

PLAN_PDF_STORAGE=blob
BLOB_READ_WRITE_TOKEN=
BLOB_STORE_ID=
